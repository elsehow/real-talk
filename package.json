{
  "name": "real-talk",
  "version": "1.0.2",
  "description": "a light protocol for encrypted, decrypting, signing and verifying JSON objects, with messages that can be easily serialized & sent over the wire (converted to/from Uint8Arrays underneath)",
  "main": "src/index.js",
  "scripts": {
    "test": "tape test.js | tap-spec",
    "watch": "npm-watch"
  },
  "watch": {
    "test": "*.js"
  },
  "author": "elsehow",
  "license": "BSD-2-Clause",
  "dependencies": {
    "halite": "^1.0.2",
    "json-buffer": "^2.0.11",
    "typedarray-to-buffer": "^3.1.2"
  },
  "devDependencies": {
    "npm-watch": "^0.1.1",
    "tap-spec": "^4.1.1",
    "tape": "^4.5.1"
  }
}
